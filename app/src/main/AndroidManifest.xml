<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dbsgproject">
    <!-- Retrofit -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".recipe.Recipe_Detail_Activity"></activity>
        <!-- SplashActivity : 앱 로딩시 초기화면 -->
        <activity
            android:name=".loading.SplashActivity"
            android:screenOrientation="fullSensor"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main_hamburger.LoginActivity" />
        <activity android:name=".main.MainActivity" />
        <activity android:name=".search.SearchActivity" />
        <activity android:name=".recipe.CookingActivity" />
        <activity android:name=".recipe.Main_recipe_Activity" />
        <activity android:name=".review.UserReview" />
        <activity android:name=".main_hamburger.PeristalsisLoginActivity" />
        <activity android:name=".main_hamburger.NicknameOverlapTestActivity" />
    </application>

</manifest>