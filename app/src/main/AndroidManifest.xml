<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dbsgproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Total_RecipeActivity"></activity>
        <activity android:name=".SearchActivity" /> <!-- SplashActivity : 앱 로딩시 초기화면 -->
         <activity android:name=".CookingActivity"></activity>
        <activity android:name=".Main_recipe_Activity" />
        <activity android:name=".UserReview" />
        <activity android:name=".PeristalsisLoginActivity" />
        <activity android:name=".NicknameOverlapTestActivity" /> <!-- SplashActivity : 앱 로딩시 초기화면 -->
        <activity android:name=".CookingActivity"></activity>
        <activity android:name=".Main_recipe_Activity" />
        <activity android:name=".UserReview" />
        <activity android:name=".PeristalsisLoginActivity" />
        <activity android:name=".NicknameOverlapTestActivity" /> <!-- SplashActivity : 앱 로딩시 초기화면 -->
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="fullSensor"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>